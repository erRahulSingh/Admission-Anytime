"use client";

import React from "react";

// Stats bar is now integrated directly into the Hero section
// so it appears on the first screen without scrolling.
export default function StatsSection() {
  return null;
}
